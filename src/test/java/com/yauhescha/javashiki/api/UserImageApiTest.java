package com.yauhescha.javashiki.api;

import org.junit.Test;

import static org.junit.Assert.*;

public class UserImageApiTest extends CommonTestData {

    @Test
    public void postImage() {
        // This test requires a valid image file
        // Uncomment and provide a valid image path to test
        // File image = new File("path/to/image.png");
        // ImageResponse response = userImageApi.postImage(image);
        // assertNotNull(response);
        assertTrue(true);
    }
}
